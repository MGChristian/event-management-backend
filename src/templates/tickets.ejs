<!DOCTYPE html>
<html>
  <head>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        padding: 20px;
      }
      .ticket-card {
        background: white;
        max-width: 400px;
        margin: 0 auto;
        padding: 20px;
        border-radius: 10px;
        text-align: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      .header {
        color: #333;
        margin-bottom: 5px;
      }
      .event-title {
        color: #007bff;
        margin-top: 0;
      }
      .date-box {
        background: #f9f9f9;
        padding: 10px;
        margin: 15px 0;
        border-radius: 5px;
        font-size: 14px;
        color: #555;
      }
      .qr-box {
        margin: 20px 0;
        border: 2px dashed #ccc;
        padding: 10px;
        display: inline-block;
      }
      .footer {
        font-size: 12px;
        color: #777;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="ticket-card">
      <h1 class="header">You're In! 🎟️</h1>
      <p>Hi <strong><%= userName %></strong>,</p>

      <p>You are confirmed for:</p>
      <h2 class="event-title"><%= eventName %></h2>

      <div class="date-box">
        <p><strong>Start:</strong> <%= eventDateStart %></p>
        <p><strong>End:</strong> <%= eventDateEnd %></p>
      </div>

      <div class="qr-box">
        <img src="cid:ticket_qr" alt="Ticket QR Code" width="150" />
      </div>

      <p>Please show this QR code at the entrance.</p>

      <div class="footer">Ticket ID: <%= ticketId %></div>
    </div>
  </body>
</html>
